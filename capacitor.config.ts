import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PRY10110182-MOVIL',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
